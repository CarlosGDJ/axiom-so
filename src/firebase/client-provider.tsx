'use client';

import type { ReactNode } from 'react';

// Stub — Firebase context replaced by NextAuth SessionProvider
export function FirebaseClientProvider({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
