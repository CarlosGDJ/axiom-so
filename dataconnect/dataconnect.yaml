specVersion: "v1beta"
serviceId: "axiom"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "axiom-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
